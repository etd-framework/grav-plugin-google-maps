<script src="https://maps.googleapis.com/maps/api/js{{ apikeystring }}"></script>
<script src="{{ url('plugin://google-maps/js/google-maps.js') }}" async defer></script>
<div id="map-{{ hash }}" class="google-map" style="width: {{ width }}; height: {{ height }}"
     data-lat="{{ lat }}"
     data-lng="{{ lng }}"
     data-zoom="{{ zoom }}"
     data-scrollwheel="{{ scrollwheel }}"
     data-draggable="{{ draggable }}"
     data-pancontrol="{{ pancontrol }}"
     data-iconurl="{{ iconurl }}"
     data-infowindow="{{ infowindow }}">
</div>